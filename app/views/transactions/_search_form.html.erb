<%= form_with url: root_path, method: :get, class: "d-flex" do |f| %>
  <%= f.select(:difficulty, options_for_select(["Today", "7 days", "30 days"], params[:query]), {},{ class: 'form-control' }) %>
<% end %>